import { css } from "@emotion/core";

const styles = css({
  display: "flex",
  flex: 1,
  justifyContent: "center",
  alignItems: "center",
  height: "100%",
  ".logo *": {
    width: "75px",
    height: "75px",
  },
});

export default styles;
